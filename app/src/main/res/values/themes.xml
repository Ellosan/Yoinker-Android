<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Yoinker" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/yk_bg</item>
        <item name="android:navigationBarColor">@color/yk_bg</item>
        <item name="android:windowBackground">@color/yk_bg</item>
    </style>

    <!-- The share sheet floats over whatever app you shared from. -->
    <style name="Theme.Yoinker.Sheet" parent="Theme.Yoinker">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
